#!/bin/bash
textual console -x SYSTEM -x EVENT -x INFO -x DEBUG

# to connect, use:
#
# textual run --dev my_script.py
#
# Inside of the script you can use "print", once the app has been mounted, to
# display things to this console
